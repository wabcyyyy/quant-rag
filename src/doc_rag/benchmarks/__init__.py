"""外部基准适配层（每个基准一个模块）。

目前只有 RGB（AAAI 2024 中文子集）：见 `rgb.py` 的模块 docstring——它写清了为什么
要跑外部基准、哪些地方必须逐字复刻官方行为、以及有意偏离的地方。

纪律：**基准数据副本不入库**（RGB 是 CC BY-NC-SA 4.0，非商用）。这里只放代码，
抓取与版本记录由 `doc-rag bench-rgb-fetch` 负责。
"""
