# 复制为 .env 并填写（.env 已被 gitignore，严禁提交）
#
# ── LLM：OpenRouter 免费档（openrouter.ai → Keys 新建）──────────────────────
#   免费模型（2026-09 实测可用，模型名以 openrouter.ai/models 为准）：
#     z-ai/glm-5.2:free                     GLM 5.2（中文强，32k 上下文）★默认
#     inclusionai/ling-3.0-flash-fin:free   Ling 3.0 Flash（中文，262k）
#     nvidia/nemotron-3-super-120b-a12b:free Nemotron 3 Super（120B，262k）
#     google/gemma-4-31b-it:free            Gemma 4 31B（262k）
#   注意：
#   1) 免费档有速率限制（约 20 req/min），代码已带指数退避重试；
#      大批量评估建议分批或降低并发
#   2) 免费档通常要求账号开启「允许用于训练/日志」——公司文档出域前先确认合规
#      （PLAN §8 敏感批次出域审批）
#   3) 切换 judge 模型后，RAGAS 指标与旧基线不可直接比较（PLAN §5.3 口径）
#
# ── Embedding / Rerank：SiliconFlow（OpenRouter 不提供这两类接口）───────────
#   BAAI/bge-m3（dense，1024 维）+ BAAI/bge-reranker-v2-m3

DOC_RAG_LLM_BASE_URL=https://openrouter.ai/api/v1
DOC_RAG_LLM_API_KEY=
DOC_RAG_LLM_MODEL=z-ai/glm-5.2:free

DOC_RAG_EMBEDDING_BASE_URL=https://api.siliconflow.cn/v1
DOC_RAG_EMBEDDING_API_KEY=
